[package]
name = "nop-iam-passwords"
version = "0.1.0"
edition = "2024"

[dependencies]
nop-config = { path = "../nop-config" }
argon2 = "0.5"
hex = "0.4"
password-hash = { version = "0.5", features = ["getrandom"] }
serde = { version = "1.0", features = ["derive"] }
