services:
  nopressure:
    build:
      context: ../..
      dockerfile: examples/docker/Dockerfile
    working_dir: /app
    volumes:
      - ../data:/data
    ports:
      - "5466:5466"
    environment:
      - RUST_LOG=info
